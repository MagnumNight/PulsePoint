from django.apps import AppConfig

# Class: ResourcesConfig - Configuration for the Resources app
class ResourcesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "resources"
